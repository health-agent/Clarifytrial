# 구조화 조건에서 모델 호출 방식 비교

기존 평가와 겹치지 않는 공개 시험을 사용해 합성 환자 25명, 환자–시험 판단 75개를 만들었다. 정답은 현재 판정 코드와 별도로 만든 계산표에 먼저 고정했다. 모든 조건은 수치·날짜·참거짓으로 구조화돼 코드가 직접 계산할 수 있다.

같은 환자와 같은 시험, 같은 질문 선택 방법을 사용하고 모델을 부르는 방식만 바꿨다.

| 모델 호출 구조 | 최종 상태 일치 | 실제 후보 확정 | 결국 제외될 후보 정리 | 모델 출력 대신 코드값을 쓴 횟수 | 별도 검토 | 실행 실패 | 외부 모델 호출 | 전체 토큰 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 구조화 규칙만 사용 | 100.0% | 36/36개 | 23/23개 | 0개 | 0회 | 0명 | 0회 | 0 |
| 조건 판단 모델만 사용 | 100.0% | 36/36개 | 23/23개 | 65개 | 0회 | 0명 | 65회 | 1,033,320 |
| 코드가 순서를 통제하고 필요한 역할만 호출 | 100.0% | 36/36개 | 23/23개 | 59개 | 0회 | 0명 | 105회 | 1,565,829 |

## 결론

세 방식의 최종 결과는 같았다. 완전히 구조화된 조건에서는 외부 모델을 더 부르지 않아도 됐다. 조건 판단 모델은 1,033,320토큰, 질문 문장 작성까지 모델로 맡긴 방식은 1,565,829토큰을 사용했지만 후보 확정이나 제외 정리 수를 늘리지 않았다.

따라서 구조화된 JSON 입력은 코드가 우선 처리한다. 조건의 뜻을 코드로 정할 수 없을 때만 조건 판단 모델을 쓰고, 실제 근거 충돌이 있을 때만 별도 검토를 부른다. 이 결과를 여러 에이전트가 정확도를 높였다는 근거로 사용하지 않는다.

## 오류를 나눈 기준

- 잘못 제외: 정답에서는 남겨야 할 시험을 제외한 경우
- 성급한 확정: 아직 정보가 부족한데 참가 가능으로 확정한 경우
- 최종 상태 불일치: 후보 유지 여부와 현재 확정 상태 가운데 하나라도 다른 경우
- 실행 실패: 정해진 출력 형식이나 호출 과정에서 결과를 만들지 못한 환자
